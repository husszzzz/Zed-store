name: ForgeSignMobile
options:
  bundleIdPrefix: com.forgesign
  deploymentTarget:
    iOS: "16.4"
  createIntermediateGroups: true

# 1. قسم المكتبات (Packages): أزل التعليقات وضع رابط مكتبة الزجاج التي استخدمتها مسبقاً
# packages:
#   GlassMorphismKit:
#     url: https://github.com/مسار_مكتبة_الزجاج_هنا.git
#     from: 1.0.0

settings:
  base:
    SWIFT_VERSION: "6.0"
    IPHONEOS_DEPLOYMENT_TARGET: "16.4"
    DEVELOPMENT_TEAM: ""
    CODE_SIGNING_ALLOWED: "NO"
    CODE_SIGNING_REQUIRED: "NO"
    CODE_SIGN_IDENTITY: ""
    ENABLE_BITCODE: "NO"
    ENABLE_DEBUG_DYLIB: "NO"

targets:
  ForgeSignMobile:
    type: application
    platform: iOS
    sources:
      - path: App
      - path: Bridge
      - path: vendor/zsign
        excludes:
          - "zsign.cpp"
          - "third-party/minizip/iowin32.c"
          - "third-party/minizip/iowin32.h"
          - "third-party/minizip/mztools.c"
          - "third-party/minizip/mztools.h"
      # 2. إذا كانت ملفات تأثير الزجاج موجودة في مجلد محلي داخل المشروع (وليس مكتبة خارجية)، أضف مساره هنا:
      # - path: vendor/GlassUI 
      
    resources:
      - path: App/Resources
      
    # 3. ربط المكتبة بالتطبيق (أزل التعليقات إذا استخدمت خطوة رقم 1)
    # dependencies:
    #   - package: GlassMorphismKit
      
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.istore.mobile
        PRODUCT_NAME: iStore
        INFOPLIST_FILE: Info.plist
        GENERATE_INFOPLIST_FILE: NO
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        TARGETED_DEVICE_FAMILY: "1,2"
        SWIFT_OBJC_BRIDGING_HEADER: Bridge/ForgeSignBridge.h
        SWIFT_ACTIVE_COMPILATION_CONDITIONS: FORGE_BRIDGE
        CLANG_CXX_LANGUAGE_STANDARD: c++11
        CLANG_CXX_LIBRARY: libc++
        GCC_C_LANGUAGE_STANDARD: gnu11
        HEADER_SEARCH_PATHS:
          - $(SRCROOT)/vendor/zsign
          - $(SRCROOT)/vendor/zsign/common
          - $(SRCROOT)/vendor/zsign/third-party/zlib
          - $(SRCROOT)/vendor/openssl/include
        LIBRARY_SEARCH_PATHS:
          - $(SRCROOT)/vendor/openssl
        OTHER_LDFLAGS:
          - -lcrypto
          - -lssl
        SWIFT_OPTIMIZATION_LEVEL: -Onone
        GCC_PREPROCESSOR_DEFINITIONS:
          - ZSIGN_VERSION=0.1.0

  ForgeSignUISim:
    type: application
    platform: iOS
    sources:
      - path: App
    resources:
      - path: App/Resources
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.forgesign.mobile.uisim
        PRODUCT_NAME: ForgeSignUISim
        INFOPLIST_FILE: Info.plist
        GENERATE_INFOPLIST_FILE: NO
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        TARGETED_DEVICE_FAMILY: "1,2"
